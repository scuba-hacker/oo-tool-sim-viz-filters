/************************************************************************************** * HEADER FILE: system_process.h * PURPOSE		: declare system_process class **************************************************************************************/#ifndef _system_process_h#define _system_process_h#include "ulist.h"#include "stringobject.h"#include "newstring.h"class system_process{	public:		string_class calc_name;											// Ref. to calculator for evaluating		string_class data_set_name;									// References data_set in data manager		string_class graph_spec_name;								// References graph_spec in graph manager		ulist<string_object> set_input_names;				// References set_inputs in data manager		ulist<string_object> set_input_fields;			// References field names in data_set		ulist<string_object> map_names;							// References IO_maps in data manager				// Function for reading names from stream and storing in one of the lists		void load_list(istream &input_stream, int list);		// Overloaded output operator for system_process class		friend ostream& operator<<(ostream& output_stream, system_process& output_map);};#endif